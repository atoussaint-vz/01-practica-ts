// Ejercicio 30: función flecha + split()
const dividirOracion = (oracion: string): string[] => oracion.split(" ");

console.log(dividirOracion("Hola mundo desde TypeScript"));
// ["Hola", "mundo", "desde", "TypeScript"]
