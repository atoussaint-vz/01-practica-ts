// Ejercicio 18: lastIndexOf() para obtener la última aparición de un valor
const ultimaAparicion = (arr: number[], valor: number): number => arr.lastIndexOf(valor);

console.log("18:", ultimaAparicion([1, 2, 3, 2, 4], 2)); // 3
